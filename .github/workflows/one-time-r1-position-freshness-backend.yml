name: Retired R1 Position Freshness Backend Generator

on:
  push:
    branches:
      - feat/r1-position-freshness

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo retired
